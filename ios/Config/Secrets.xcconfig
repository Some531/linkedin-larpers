// Build-time secrets wiring. This file IS committed — put real values in
// Secrets.local.xcconfig (git-ignored), which overrides the empty defaults
// below. See ios/README.md.

ANTHROPIC_API_KEY =

#include? "Secrets.local.xcconfig"
